server:
  port: 9091

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false # false 表示不向注册中心注册自己
    fetch-registry: false # 表示是自己端就是注册中心 ，我的职责就是维护服务实例
    service-url:
      defaultZone: http://localhost:9090/eureka/

